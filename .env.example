PEC_DB_HOST=localhost
PEC_DB_PORT=54323
PEC_DB_NAME=postgres
PEC_DB_USER=esus_monsenhor
PEC_DB_PASSWORD=pass

# MCP HTTP (servidor) - porta separada da UI
MCP_HTTP_HOST=127.0.0.1
MCP_HTTP_PORT=5174
# Alias compatível com FastMCP (usado internamente pelo server)
FASTMCP_HOST=127.0.0.1
FASTMCP_PORT=5174

# UI FastAPI/Jinja (frontend)
UI_HOST=0.0.0.0
UI_PORT=8000

# Claude / Anthropic (frontend de chat)
# Informe o API key para evitar digitar na UI; modelo é configurável na interface.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Altere estes valores em produção/QA para credenciais apropriadas.
